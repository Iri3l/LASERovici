/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  // images: { unoptimized: true }, // uncomment if you use next/image on static hosting
};

module.exports = nextConfig;
